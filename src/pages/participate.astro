---
/**
 * Allowlisted F4 form_signup_shell — `/participate` for synthetic mission tenant.
 */
export const prerender = false

import MissionFormSignupShell from "../components/mission/MissionFormSignupShell.astro"
import { loadAllowlistedMissionPage } from "../lib/loadAllowlistedMissionPage.js"

const mission = await loadAllowlistedMissionPage(
  Astro.request,
  Astro.locals,
  "/participate",
  "form_signup_shell",
)

if (!mission.ok) {
  return new Response("Not found", {
    status: mission.status || 404,
    headers: { "Content-Type": "text/plain; charset=utf-8", "Cache-Control": "no-store" },
  })
}
---

<MissionFormSignupShell
  title={mission.route.title || "Participar"}
  heading={mission.route.heading || mission.route.title || "Participar"}
  body={mission.route.body || ""}
  host={mission.host}
  path="/participate"
  noindex={true}
  seo={mission.seo}
  chrome={mission.chrome}
/>
