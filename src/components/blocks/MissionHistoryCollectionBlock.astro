---
/**
 * F4 mission — completed initiatives / history collection.
 */
const { block } = Astro.props as {
  block: {
    type?: string
    content?: {
      title?: string | null
      items?: {
        id?: string
        title?: string
        subtitle?: string | null
        imageUrl?: string | null
        imageAlt?: string | null
      }[]
    }
  }
}

const content = block?.content ?? {}
const title =
  typeof content.title === "string" && content.title.trim()
    ? content.title.trim()
    : "Iniciativas concluídas"
const items = Array.isArray(content.items)
  ? content.items.filter((item) => item && typeof item.title === "string" && item.title.trim())
  : []
const show = items.length > 0
---

{show && (
  <section class="mission-history" aria-labelledby="mission-history-title">
    <div class="mission-history__container">
      <h2 id="mission-history-title" class="mission-history__title">
        {title}
      </h2>
      <ul class="mission-history__list">
        {items.map((item) => {
          const alt =
            typeof item.imageAlt === "string" && item.imageAlt.trim()
              ? item.imageAlt.trim()
              : item.title || "Iniciativa"
          return (
            <li class="mission-history__item">
              {item.imageUrl && (
                <img
                  class="mission-history__image"
                  src={item.imageUrl}
                  alt={alt}
                  loading="lazy"
                />
              )}
              <div class="mission-history__copy">
                <h3 class="mission-history__item-title">{item.title}</h3>
                {item.subtitle && (
                  <p class="mission-history__subtitle">{item.subtitle}</p>
                )}
              </div>
            </li>
          )
        })}
      </ul>
    </div>
  </section>
)}

<style>
  .mission-history {
    padding: 3rem 0;
    background: var(--site-color-background, hsl(40 16% 98%));
    color: var(--site-color-foreground, hsl(220 15% 16%));
  }
  .mission-history__container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: grid;
    gap: 1.25rem;
  }
  .mission-history__title {
    margin: 0;
    font-size: 1.35rem;
    font-weight: 650;
    color: var(--site-color-primary, hsl(220 20% 20%));
  }
  .mission-history__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1.25rem;
  }
  @media (min-width: 720px) {
    .mission-history__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  .mission-history__item {
    display: grid;
    gap: 0.75rem;
  }
  .mission-history__image {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    aspect-ratio: 16 / 10;
  }
  .mission-history__item-title {
    margin: 0;
    font-size: 1.05rem;
  }
  .mission-history__subtitle {
    margin: 0.25rem 0 0;
    opacity: 0.8;
    font-size: 0.95rem;
  }
</style>
